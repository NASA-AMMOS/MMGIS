{
  "name": "Labels",
  "type": "layerattachment",
  "attachmentId": "labels",
  "version": "1.0.0",
  "tier": "core",
  "overridable": false,
  "description": "A text label beside each of the host layer's features, including its coordinate markers. Shown and hidden through its own label layer rather than by adding and removing a map layer.",
  "capabilities": {
    "renderers": {
      "map": {
        "engines": ["leaflet"]
      },
      "globe": false
    },
    "host": {
      "order": 0,
      "buildsAfterSiblings": true
    }
  },
  "paths": {
    "plugin": "./labels"
  },
  "applicableLayerTypes": ["vector", "query"]
}
