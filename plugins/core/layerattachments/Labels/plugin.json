{
  "name": "Labels",
  "type": "layerattachment",
  "attachmentId": "labels",
  "version": "1.0.0",
  "tier": "core",
  "overridable": false,
  "description": "Per-feature text labels drawn beside their host layer's features on the 2D map. Shown and hidden through its own label layer rather than by adding/removing a map layer.",
  "capabilities": {
    "renderers": {
      "map": {
        "engines": [
          "leaflet"
        ]
      },
      "globe": false
    }
  },
  "paths": {
    "plugin": "./labels"
  },
  "applicableLayerTypes": [
    "vector",
    "query"
  ]
}
