{
  "name": "UncertaintyEllipses",
  "type": "layerattachment",
  "attachmentId": "uncertainty_ellipses",
  "version": "1.0.0",
  "tier": "core",
  "overridable": false,
  "configPath": "variables.markerAttachments.uncertainty",
  "description": "Ellipses about the host layer's point features indicating positional uncertainty, sized from X/Y uncertainty properties: a polygon overlay on the 2D map plus a vertical curtain and a clamped surface on the globe.",
  "capabilities": {
    "renderers": {
      "map": {
        "engines": ["leaflet"]
      },
      "globe": {
        "engines": ["lithosphere"]
      }
    },
    "host": {
      "order": 2
    }
  },
  "metaconfig": "./metaconfig.json",
  "paths": {
    "plugin": "./uncertaintyEllipses"
  },
  "applicableLayerTypes": ["vector", "query"]
}
