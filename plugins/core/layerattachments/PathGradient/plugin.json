{
  "name": "PathGradient",
  "type": "layerattachment",
  "attachmentId": "path_gradient",
  "version": "1.0.0",
  "tier": "core",
  "overridable": false,
  "description": "Colors a host line layer's path by one of its properties. On the globe it replaces its host: the gradient primitive it builds is the host's geometry, so the host must not also be drawn.",
  "capabilities": {
    "renderers": {
      "map": {
        "engines": [
          "leaflet"
        ]
      },
      "globe": {
        "engines": [
          "cesium"
        ]
      }
    },
    "globe": {
      "suppressesHost": true
    }
  },
  "paths": {
    "plugin": "./pathGradient"
  }
}
