{
  "name": "Pairings",
  "type": "layerattachment",
  "attachmentId": "pairings",
  "version": "1.0.0",
  "tier": "core",
  "overridable": false,
  "configPath": "variables.layerAttachments.pairings",
  "description": "Links this layer's features to the matching features of other layers: a line between each pair on the map, and the azimuth-elevation relationship those pairs are drawn with in the Viewer. Redraws itself whenever one of the layers it pairs with is toggled.",
  "capabilities": {
    "renderers": {
      "map": {
        "engines": ["leaflet"]
      },
      "globe": false
    },
    "host": {
      "order": 1
    }
  },
  "paths": {
    "plugin": "./pairings"
  },
  "applicableLayerTypes": ["vector", "query"]
}
