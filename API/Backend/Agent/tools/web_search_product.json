{
  "name": "web_search_product",
  "description": "Search the web for a NASA/NOAA product and return 2–4 links with a brief summary.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "minLength": 2 }
    },
    "required": ["query"],
    "additionalProperties": false
  },
  "modelParameters": {
    "type": "object",
    "properties": {
      "query": { "type": "string" }
    },
    "required": ["query"],
    "additionalProperties": false
  },
  "execution": {
    "adapter": "openapi",
    "operation": "web_search_product",
    "ui": { "type": "web_search_suggest" }
  }
}
